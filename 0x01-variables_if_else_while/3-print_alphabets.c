#include <stdio.h>

/**
* Main - prints the alphabet in lowercase, and then in uppercase
*
*Return: Always 0
*/

int main(void)
{
	int n;

	for (n = '0'; n <= '9'; ch++)
		putchar(n);
	
	putchar('\n');
	return (0);
}
