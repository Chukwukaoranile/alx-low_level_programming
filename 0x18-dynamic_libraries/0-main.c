#include "./100-operations.so"
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: Always Exit_SUCCESS.
 */

int main(void)
{
	printf("%d\n", _strlen("100-operations"));
	return (Exit_SUCCESS);
}
