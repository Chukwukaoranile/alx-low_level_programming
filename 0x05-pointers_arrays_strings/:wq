#include "main.h"

/**
 * _puts - Function that prints a string, followed by a new line, to stdout.
 * @str: to be printed
 */

void _puts(char *str)
{
	while (*str != '\0')
	{
		_puts(*str);
		str++;
	}
	_puts('\n');
}
